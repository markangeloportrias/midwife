# UI/UX redesign - Admin Dashboard (UI only)

## Plan steps

1. Confirm `admin-dashboard.html` contains all UI + JS; do UI-only edits.
2. Update CSS variables + global theme to match required palette/typography.
3. Redesign sticky header (90–100px) with dual logos + title/subtitle + admin profile.
4. Redesign sidebar as floating rounded panel; update active/inactive/hover states + badges.
5. Convert top of Instructor section into a hero card (welcome + illustration).
6. Add statistics section (4 cards) with proper responsive grid.
7. Redesign main content container header, form layout (horizontal desktop), buttons (with icons), and modern table styling.
8. Ensure mobile/tablet responsiveness (stack stats, responsive form, collapsible sidebar styles, table horizontal scroll).
9. Keep all existing IDs, event handlers, JS logic, routes, and API/database calls unchanged.
10. Smoke test in browser: navigation, instructor CRUD, logout modal, option modal.
